server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:}
    username: ${DB_USERNAME:}
    password: ${DB_PW:}
  jpa:
    show-sql: true

auth:
  time: 6000
  secret:
    user: ${SECRET_USER:}
    admin: ${SECRET_ADMIN:}